# Используем официальный образ Node.js
FROM node:18

# Устанавливаем рабочую директорию
WORKDIR /frontend

# Команда по умолчанию для удержания контейнера в активном состоянии
CMD ["tail", "-f", "/dev/null"]
