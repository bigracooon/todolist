# Install
Add this info to your composer.json
```shell
    "repositories": [
        {
            "type": "path",
            "url": "/libs/checker"
        },
    ]
```
and run
```shell
composer install balashov/checker
```